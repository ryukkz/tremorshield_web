services:
  tremorshield:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data   # participant CSVs/zips persist on the host
    env_file:
      - .env
    restart: unless-stopped
